define([], function() {
    var Context = function() {
    };
    Context.prototype = {
    };
    Context.acquire_id = function(thing) {
        console.warn("acquire_id unimplemented");
        return 0;
    };
    return Context;
});
