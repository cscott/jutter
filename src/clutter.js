define(["./actor", "./color", "./stage"], function(Actor, Color, Stage) {

    var init = function(args) {
    };

    var main = function() {
    };

    return {
        init: init,
        Actor: Actor,
        Color: Color,
        Stage: Stage,
        main: main
    };
});
